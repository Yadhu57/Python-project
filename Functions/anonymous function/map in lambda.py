# E1:
li = [5, 10, 15, 20, 25]
final_list = list(map(lambda x: x * 2, li))
print(final_list)

#----------------------------------------------------

# Eg2:

l2 = ['cat', 'dog', 'cow', 'hen', 'parrot']
upper_list = list(map(lambda y: y.upper(), l2))
print(upper_list)